# 배열 다루기

```
// 총 개수
let members = ['쿤갈레', 'Zerrard66', '우리생각해써',
'흙토끼', 'End Miracle'];


console.log(members.length);
console.log(members['length'];

배열이 가지고 있는 요소의 총 개수를 표시해 준다

➡️ 5
```

```
// 수정, 추가
let members = ['쿤갈레', 'Zerrard66', '우리생각해써',
'흙토끼', 'End Miracle'];


members[5] = '이현석';
consol.log(members[5]);

이미 존재하는 요소에 접근해서 값을 할당하게 되면 값이 수정이 되고 값이 없으면 추가가 된다

➡️ 이현석
```
